[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "threedots"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "gymnasium",
    "numpy",
]
